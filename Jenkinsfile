pipeline {
    agent any

    stages {
        stage('Checkout Verification') {
            steps {
                echo 'Successfully pulled code from GitHub'
            }
        }

        stage('Git Practice') {
            steps {
                echo 'Jenkins is executing pipeline from GitHub repository'
            }
        }
    }
}
